import {Toolbar, Typography, AppBar} from "@material-ui/core";

export const Header = () => (
    <AppBar>
        <Toolbar>
            <Typography variant={"h6"}>Find Info</Typography>
        </Toolbar>
    </AppBar>
)